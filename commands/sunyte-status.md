---
description: Sunyte at a glance — tier, active guardrails, spend limit, log integrity
allowed-tools: Bash
---

Run:

```
python3 "${CLAUDE_PLUGIN_ROOT}/sunyte/cli.py" status
```

Relay the output. If the tier is FREE, mention that `/sunyte-tier` explains the
Team and Compliance features (Slack alerts, exports, cross-session search, signed
audit manifests).
